{
  "name": "healthycloud-e2e-playwright",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "npx playwright test",
    "test:api": "npx playwright test tests/api",
    "test:ui": "npx playwright test tests/ui",
    "test:report:playwright": "npx playwright show-report ",
    "test:report:allure": "allure generate allure-results --clean && allure open",
    "clean:allure": "rm -rf allure-results allure-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "@types/node": "^22.10.2",
    "allure-playwright": "^3.3.0"
  },
  "dependencies": {
    "dotenv": "^17.0.1",
    "pino": "^9.7.0",
    "pino-pretty": "^13.0.0"
  }
}
